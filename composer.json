{
    "require": {
        "vlucas/phpdotenv": "^5.6",
        "firebase/php-jwt": "^6.11"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^2.1"
    },
    "scripts": {
        "phpstan": "php -d memory_limit=1G vendor/bin/phpstan analyse --memory-limit=1G"
    }
}
